/**
 * eZmax API Definition
 * TODO Api description
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: support@ezmax.ca
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

#include "OAIActivesessionApi.h"
#include "OAIHelpers.h"

#include <QJsonArray>
#include <QJsonDocument>

namespace OpenAPI {

OAIActivesessionApi::OAIActivesessionApi(const QString &scheme, const QString &host, int port, const QString &basePath, const int timeOut)
    : _scheme(scheme),
      _host(host),
      _port(port),
      _basePath(basePath),
      _timeOut(timeOut),
      _manager(nullptr),
      isResponseCompressionEnabled(false),
      isRequestCompressionEnabled(false) {}

OAIActivesessionApi::~OAIActivesessionApi() {
}

void OAIActivesessionApi::setScheme(const QString &scheme) {
    _scheme = scheme;
}

void OAIActivesessionApi::setHost(const QString &host) {
    _host = host;
}

void OAIActivesessionApi::setPort(int port) {
    _port = port;
}

void OAIActivesessionApi::setBasePath(const QString &basePath) {
    _basePath = basePath;
}

void OAIActivesessionApi::setTimeOut(const int timeOut) {
    _timeOut = timeOut;
}

void OAIActivesessionApi::setWorkingDirectory(const QString &path) {
    _workingDirectory = path;
}

void OAIActivesessionApi::setNetworkAccessManager(QNetworkAccessManager* manager) {
    _manager = manager;  
}

void OAIActivesessionApi::addHeaders(const QString &key, const QString &value) {
    defaultHeaders.insert(key, value);
}

void OAIActivesessionApi::enableRequestCompression() {
    isRequestCompressionEnabled = true;
}

void OAIActivesessionApi::enableResponseCompression() {
    isResponseCompressionEnabled = true;
}

void OAIActivesessionApi::abortRequests(){
    emit abortRequestsSignal();
}

void OAIActivesessionApi::getCurrent() {
    QString fullPath = QString("%1://%2%3%4%5")
                           .arg(_scheme)
                           .arg(_host)
                           .arg(_port ? ":" + QString::number(_port) : "")
                           .arg(_basePath)
                           .arg("/1/object/activesession/getCurrent");

    OAIHttpRequestWorker *worker = new OAIHttpRequestWorker(this, _manager);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    OAIHttpRequestInput input(fullPath, "GET");

    foreach (QString key, this->defaultHeaders.keys()) { input.headers.insert(key, this->defaultHeaders.value(key)); }

    connect(worker, &OAIHttpRequestWorker::on_execution_finished, this, &OAIActivesessionApi::getCurrentCallback);
    connect(this, &OAIActivesessionApi::abortRequestsSignal, worker, &QObject::deleteLater); 
    worker->execute(&input);
}

void OAIActivesessionApi::getCurrentCallback(OAIHttpRequestWorker *worker) {
    QString msg;
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type == QNetworkReply::NoError) {
        msg = QString("Success! %1 bytes").arg(worker->response.length());
    } else {
        msg = "Error: " + worker->error_str;
        error_str = QString("%1, %2").arg(worker->error_str).arg(QString(worker->response));
    }
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        emit getCurrentSignal();
        emit getCurrentSignalFull(worker);
    } else {
        emit getCurrentSignalE(error_type, error_str);
        emit getCurrentSignalEFull(worker, error_type, error_str);
    }
}

} // namespace OpenAPI
